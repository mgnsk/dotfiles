#!/bin/env bash

set -eo pipefail

# Called by Neomake.

if npm list eslint &>/dev/null; then
	# Use eslint from project dir if available.
	npx --offline eslint "$@"
else
	# Otherwise use global eslint.
	/usr/bin/eslint "$@"
fi
