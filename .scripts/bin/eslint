#!/bin/env bash

# Called by nvim-lint.

if npm list --depth=0 eslint &>/dev/null; then
	# Use eslint from project dir if available.
	exec npx --offline eslint "$@" 2>/dev/null
else
	# Otherwise use global eslint.
	exec /usr/bin/eslint "$@" 2>/dev/null
fi
